import streamlit as st
import random
import datetime

# ตั้งค่าหน้าตาแอพ
st.set_page_config(page_title="Lucky 246 (มหาเฮง)", page_icon="🔮")

st.title("🔮 Lucky 246 (มหาเฮง)")
st.subheader("เขย่าเซียมซีดิจิทัล & รับเลขนำโชค")

# ส่วนรับข้อมูล
name = st.text_input("ระบุชื่อ-นามสกุลจริง (เพื่อความแม่นยำ)")
birthday = st.date_input("วันเดือนปีเกิด", min_value=datetime.date(1950, 1, 1))

# รายการคำทำนายสไตล์เซียมซี
fortune_texts = [
    "ใบที่ ๑: ช่วงนี้บุญบารมีหนุนนำ หยิบจับอะไรเป็นเงินเป็นทอง มีเกณฑ์ได้ลาภลอยแบบงงๆ",
    "ใบที่ ๒: อดทนอีกนิด ฟ้าหลังฝนย่อมสวยงามเสมอ สิ่งที่รอคอยกำลังจะมาถึงในเร็ววัน",
    "ใบที่ ๓: มีคนคอยอุปถัมภ์ค้ำชู โดยเฉพาะผู้ใหญ่จะให้ความเมตตาเป็นพิเศษ",
    "ใบที่ ๔: ระวังเรื่องการใช้จ่ายเงินทองไหลออกง่าย แต่จะมีโชคด้านความรักมาทดแทน",
    "ใบที่ ๕: ศัตรูจะแพ้พ่าย การงานที่ติดขัดจะคลี่คลายด้วยสติปัญญาของท่านเอง"
]

if st.button("🔮 สาธุ... ขอเขย่าเซียมซี"):
    # ระบบเช็กชื่อมั่ว (ต้องมีช่องว่างระหว่างชื่อ-นามสกุล และห้ามสั้นเกินไป)
    if not name or " " not in name or len(name) < 5:
        st.error("⚠️ คำเตือนจากสิ่งศักดิ์สิทธิ์: กรุณาระบุชื่อและนามสกุลจริงให้ถูกต้อง (ต้องมีเว้นวรรค) มิฉะนั้นดวงจะเคลื่อน!")
    else:
        # ใช้ชื่อเป็นตัวกำหนดดวง (Seed) เพื่อให้คนเดิมได้ดวงเดิมในวันนั้น
        random.seed(name + str(birthday) + str(datetime.date.today()))
        
        selected_fortune = random.choice(fortune_texts)
        lucky_numbers = [random.randint(0, 9) for _ in range(3)]
        lucky_str = "".join(map(str, lucky_numbers))
        
        st.divider()
        st.balloons() # เอฟเฟกต์ฉลองความเฮง
        
        st.markdown(f"### 📜 คำทำนายของคุณ {name}")
        st.info(selected_fortune)
        
        col1, col2 = st.columns(2)
        with col1:
            st.metric("✨ เลขนำโชค", lucky_str)
        with col2:
            st.metric("💰 ระดับโชคลาภ", f"{random.randint(80, 100)}%")

        st.success("🙏 สาธุ... ขอให้สมหวังดั่งคำทำนาย")
        st.write("---")
        st.caption("สนับสนุนค่าไฟแอดมินกี้: [ใส่ลิงก์ PromptPay หรือ Affiliate ของกี้ตรงนี้]")
