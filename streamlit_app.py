import streamlit as st
import random
import datetime

# ตั้งค่าหน้าตาแอพ
st.set_page_config(page_title="Lucky 246 (มหาเฮง)", page_icon="🔮")

st.title("🔮 Lucky 246 (มหาเฮง)")
st.subheader("ตรวจดวงชะตา & รับเลขนำโชคเฉพาะคุณ")

# ส่วนรับข้อมูล (Input)
name = st.text_input("ชื่อ-นามสกุล ของคุณ")
birthday = st.date_input("วันเดือนปีเกิด", min_value=datetime.date(1950, 1, 1))

if st.button("🔮 วิเคราะห์ดวงและสุ่มเลขมงคล"):
    if name:
        # Logic จำลองการวิเคราะห์ (ใช้ชื่อและวันเกิดมาเป็น Seed ให้ดูแม่นยำ)
        random.seed(name + str(birthday))
        
        luck_score = random.randint(70, 100)
        lucky_numbers = [random.randint(0, 9) for _ in range(3)]
        lucky_str = "".join(map(str, lucky_numbers))
        
        colors = ["แดงมงคล", "เขียวเหนี่ยวทรัพย์", "เหลืองทอง", "ฟ้านำโชค", "ชมพูเมตตา"]
        lucky_color = random.choice(colors)

        st.success(f"สวัสดีคุณ {name}!")
        st.write(f"📊 **ระดับความเฮงวันนี้:** {luck_score}%")
        st.info(f"✨ **เลขนำโชคของคุณคือ:** {lucky_str}")
        st.warning(f"🎨 **สีมงคลเสริมดวง:** {lucky_color}")
        
        st.write("---")
        st.write("🙏 *สนับสนุนค่าไฟแอดมิน (Tip Jar):* [แสกน QR โอนผ่านพร้อมเพย์]")
        st.write("📢 *แชร์ดวงของคุณให้เพื่อนรู้ เพื่อเพิ่มบุญบารมี!*")
    else:
        st.error("กรุณาใส่ชื่อของคุณก่อนนะเพื่อน!")
